/**
 * Find the first non-repeating character in a string.
 *
 * Example - for the given string:
 * 'andifbutwrongbutifand'
 * the function output should be:
 * 'w'
 *
 * @param someString The input string to test
 */
export const findFirstNonRepeatingCharacter = (someString: string): string => {
    // Implement me!
};
