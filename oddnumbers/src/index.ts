type NumberNumberRecord = Record<number, number>;

/**
 * Find all odd numbers in the #numbers parameter and return a {@link NumberNumberRecord}
 * containing a key that is the odd number found and a value that is the
 * count of occurrences for that particular odd number.
 *
 * Example - for the given input array:
 * [1, 2, 3, 4, 233, 1]
 * the function output should be deep equal to:
 * {
 *  1: 2,
 *  3: 1,
 *  233: 1,
 * }
 *
 * @param numbers The input array of numbers to test/count
 */
export const findAndCountOddNumbers = (
    numbers: number[]
): NumberNumberRecord => {
    // Implement me!
};
